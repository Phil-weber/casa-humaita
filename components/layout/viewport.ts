import type { Viewport } from 'next';

/** Um `viewport` so para os tres root layouts — nao depende de idioma. */
export const viewport: Viewport = {
  themeColor: '#0b1620',
  width: 'device-width',
  initialScale: 1,
};
